[project]
name = "workos_frappe_integration"
version = "0.1.0"
description = "Cybranex-owned ERPNext and Frappe CRM integration hooks"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Cybranex" }]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"
